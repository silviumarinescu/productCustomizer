{
  "name": "product-customizer",
  "version": "1.0.0",
  "description": "product customizer",
  "dependencies": {
    "three": "^0.142.0"
  },
  "devDependencies": {
    "vite": "^3.0.2"
  },
  "scripts": {
    "start": "vite src --host",
    "build": "vite build src"
  }
}
